---
label: Section
hide_body: true
fields:
- name: section
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: true
    label: Title
  config:
    min: 
    max: 
    labelField: title
  label: Section
