---
label: Section
hide_body: true
fields:
- type: field_group_list
  name: section
  label: Section
  fields:
  - type: text
    name: title
    label: Title
    config:
      required: true
  - type: field_group_list
    name: dated_list
    label: Dated List
    fields:
    - type: text
      name: title
      label: Title
      config:
        required: true
    - type: text
      name: url
      label: URL
      config:
        required: true
    - name: date
      type: datetime
      label: Date
      description: ''
      config:
        required: true
        date_format: YYYY
        time_format: 
        display_utc: false
    config:
      labelField: title
  config:
    labelField: title
